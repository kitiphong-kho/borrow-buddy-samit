# Borrow Buddy: Tasks

อ้างอิง [CONTEXT.md](./CONTEXT.md) และ [design.md](./design.md) ทำตามลำดับ ติ๊กเมื่อเสร็จ

กติกาการทำงาน: JavaScript เท่านั้น, ห้ามลบไฟล์โดยไม่ถามก่อน, ตอนตั้งค่า Vite/React/Vitest ให้ตรวจเอกสารล่าสุดผ่าน Context7 ก่อน

## เฟส 1: ตั้งโปรเจ็กต์

- [x] T1.1 สร้างโปรเจ็กต์ Vite + React (เทมเพลต JavaScript) ในโฟลเดอร์ `borrow-buddy` โดยไม่ทับ `CONTEXT.md` และ `design.md`
- [x] T1.2 ติดตั้ง Vitest และเพิ่มสคริปต์ `test` ใน `package.json`
- [x] T1.3 สร้างโครงโฟลเดอร์ `src/components` และ `src/lib` ตาม design.md ข้อ 7

- **เสร็จเมื่อ:** `npm run dev` เปิดหน้าเปล่าได้ และ `npm test` รันได้

## เฟส 2: ตรรกะ (`src/lib`) ทำแบบเขียนเทสต์ก่อน

- [x] T2.1 `dateFormat.js`: แปลง `YYYY-MM-DD` เป็น พ.ศ. แบบ "24 ก.ย. 2569" + เทสต์
- [x] T2.2 `loanRules.js`: คำนวณสถานะ (ยังไม่คืน / เกินกำหนด / คืนแล้ว) รับ "วันนี้" เป็นพารามิเตอร์ + เทสต์ขอบ (วันนี้ = กำหนดคืนต้องยังไม่เกิน, วันถัดไปเกิน)
- [x] T2.3 `loanRules.js`: จำนวนวันเกินกำหนด + เทสต์
- [x] T2.4 `loanRules.js`: ตรวจความถูกต้องของ Loan (ชื่อเพื่อน/ของห้ามว่าง, กำหนดคืนและวันที่คืนจริงต้องไม่ก่อนวันที่ยืม) คืนข้อความผิดพลาดภาษาไทย + เทสต์ทุกกรณีผิด
- [x] T2.5 `loanRules.js`: จัดกลุ่ม เกินกำหนด → ยังไม่คืน → คืนแล้ว และเรียงตามกำหนดคืนใกล้สุดในแต่ละกลุ่ม + เทสต์
- [x] T2.6 `loanRules.js`: กรองตามชื่อเพื่อน + เทสต์
- [x] T2.7 `loanRules.js`: กดคืนแล้ว (ค่าเริ่มต้นวันนี้ แก้ย้อนหลังได้) และยกเลิกการคืน + เทสต์
- [x] T2.8 `storage.js`: อ่าน/เขียน Loan ใน localStorage คีย์เดียว, JSON เสียหรืออ่านไม่ได้ให้คืนรายการว่างพร้อมสัญญาณแจ้งเตือน ไม่เขียนทับข้อมูลเดิม

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด

## เฟส 3: UI

- [x] T3.1 `App.jsx`: เก็บ state Loan โหลดจาก storage ตอนเปิด บันทึกทุกครั้งที่เปลี่ยน และแสดงข้อความภาษาไทยเมื่ออ่านข้อมูลไม่ได้
- [x] T3.2 `LoanForm.jsx`: ฟอร์มเดียวใช้เพิ่มและแก้ไข (เพื่อน, ของ, วันที่ยืม ค่าเริ่มต้นวันนี้, กำหนดคืน) แสดงข้อผิดพลาดจากการตรวจ มีปุ่มยกเลิกการแก้ไข
- [x] T3.3 `SearchBox.jsx`: ช่องค้นหาตามชื่อเพื่อน
- [x] T3.4 `LoanList.jsx` และ `LoanItem.jsx`: แสดงกลุ่มและรายการตาม design.md ข้อ 6 (วันที่ พ.ศ., จำนวนวันเกินเฉพาะเกินกำหนด, วันที่คืนจริงเฉพาะคืนแล้ว)
- [x] T3.5 ปุ่มต่อรายการ: คืนแล้ว (เลือกวันที่ได้), ยกเลิกการคืน, แก้ไข ไม่มีปุ่มลบ
- [x] T3.6 `ThemeToggle.jsx`: โหมดมืด สลับได้และจำค่าไว้
- [x] T3.7 สไตล์: รองรับมือถือ, สถานะแยกด้วยข้อความและสี (ไม่พึ่งสีอย่างเดียว)

- **เสร็จเมื่อ:** ใช้งานครบทุกอย่างในขอบเขตผ่านหน้าเว็บได้

## เฟส 4: ตรวจรับ

- [x] T4.1 รัน `npm test` ผ่านทั้งหมด
- [x] T4.2 ทดลองในเบราว์เซอร์: เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วข้อมูลยังอยู่
- [x] T4.3 ทดลองกรณีขอบ: กำหนดคืนวันนี้, กำหนดคืนเมื่อวาน, กรอกวันที่ผิดกติกา, ข้อมูลใน localStorage เสีย
- [x] T4.4 ตรวจหน้าจอมือถือและโหมดมืด
- [x] T4.5 ตรวจว่าไม่มีฟีเจอร์นอกขอบเขต (ลบ, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, สำรองข้อมูล) และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

## เฟส 5: เวอร์ชัน 2 — ย้ายไป Supabase + ล็อกอิน

อ้างอิง design.md เวอร์ชัน 2 (ข้อ 3, 4, 6, 7, 8, 9) และ CONTEXT.md (บัญชีเจ้าของ, เข้าสู่ระบบ)

- [x] T5.1 สร้าง Supabase project (หรือใช้ project ที่มีอยู่) ติดตั้ง `@supabase/supabase-js` และตั้งค่า `lib/supabaseClient.js`
- [x] T5.2 สร้าง migration ตาราง `loans` ตามโมเดลข้อมูลใน design.md ข้อ 4 (`id`, `owner_id`, `friend_name`, `item_name`, `borrowed_date`, `due_date`, `returned_date`)
- [x] T5.3 เปิด RLS บนตาราง `loans` และเขียน policy `select`/`insert`/`update` ให้กรองด้วย `owner_id = auth.uid()` (ไม่มี policy `delete`)
- [x] T5.4 สร้างบัญชีเจ้าของอย่างน้อย 1 บัญชีผ่าน Supabase Dashboard/Admin API เพื่อทดสอบ (ปิดการสมัครสมาชิกเองในฝั่ง client) — ผู้ใช้สร้างบัญชีที่ 1 (`khongphinit@gmail.com`) เองผ่าน Dashboard, สร้างบัญชีที่ 2 (`owner2-test@borrowbuddy.local`) เพิ่มผ่าน SQL เพื่อทดสอบ RLS โดยเฉพาะ
- [x] T5.5 `lib/auth.js`: ฟังก์ชันเข้าสู่ระบบ/ออกจากระบบ และ subscribe สถานะ session + เทสต์ (mock Supabase client)
- [x] T5.6 `components/LoginForm.jsx`: ฟอร์มอีเมล + รหัสผ่าน แสดงข้อความผิดพลาดภาษาไทยเมื่อล็อกอินไม่สำเร็จ
- [x] T5.7 `App.jsx`: สลับแสดงหน้าเข้าสู่ระบบ/หน้าหลักตามสถานะ session, ปุ่มออกจากระบบ, แสดงอีเมลเจ้าของที่ล็อกอินอยู่
- [x] T5.8 `lib/loansApi.js`: แทนที่ `storage.js` ด้วยการอ่าน/เขียน Loan ผ่าน Supabase (select/insert/update ตาม RLS) แปลง `snake_case` ↔ `camelCase` ให้ชั้น UI ใช้รูปแบบเดิม + เทสต์ (mock client รวมกรณีเรียกล้มเหลว)
- [x] T5.9 ลบการใช้งาน `lib/storage.js`/localStorage ออกจาก `App.jsx` (ถามผู้ใช้แล้ว — เลือกเก็บไฟล์ `storage.js`/`storage.test.js` ไว้ก่อน ไม่ลบ)
- [x] T5.10 ตรวจว่า Loan ทุกอันที่สร้างใหม่มี `owner_id` ตรงกับผู้ใช้ที่ล็อกอินอยู่เสมอ ผู้ใช้แก้ `owner_id` เองไม่ได้ผ่าน UI (`createLoan` บังคับ `owner_id` จาก `session.user.id` เสมอ ไม่รับจากฟอร์ม)

- **เสร็จเมื่อ:** ล็อกอินได้ด้วยบัญชีเจ้าของที่สร้างไว้, เพิ่ม/แก้ไข/คืน/ยกเลิกคืน Loan ผ่าน Supabase ได้, สร้างบัญชีเจ้าของที่สองแล้วเห็น Loan แยกกันตามบัญชี, `npm test` ผ่านทั้งหมด — **เสร็จสมบูรณ์**

## เฟส 6: ตรวจรับเวอร์ชัน 2

ทดสอบผ่าน Playwright (chromium headless) กับ dev server จริง + ตรวจข้อมูลใน Supabase ตรง ๆ ด้วย SQL

- [x] T6.1 ทดลองล็อกอินด้วยอีเมล/รหัสผ่านผิด ต้องเห็นข้อความผิดพลาดภาษาไทย ไม่เข้าเว็บได้ — ล็อกอินด้วยอีเมล/รหัสผ่านมั่ว ได้ข้อความ "อีเมลหรือรหัสผ่านไม่ถูกต้อง" ไม่เข้าหน้าหลัก
- [x] T6.2 ทดลองสร้างบัญชีเจ้าของ 2 บัญชี ยืนยันว่าแต่ละบัญชีเห็นเฉพาะ Loan ของตัวเอง — บัญชี 1 (`khongphinit@gmail.com`) เพิ่ม Loan 1 รายการ, บัญชี 2 (`owner2-test@borrowbuddy.local`) ล็อกอินแล้วเห็น "ไม่มีรายการ" ตรวจ SQL ตรง ๆ ยืนยันว่า `owner_id` ของ Loan ตรงกับบัญชี 1 เท่านั้น
- [x] T6.3 ทดลอง refresh หน้าเว็บระหว่างล็อกอินอยู่ ต้องยังอยู่ในระบบ (session คงอยู่) — reload หน้าแล้วยัง login อยู่และเห็น Loan เดิมครบ
- [x] T6.4 ทดลองออกจากระบบแล้วพยายามเข้าหน้าหลักโดยตรง ต้องเด้งกลับหน้าเข้าสู่ระบบ — กด "ออกจากระบบ" แล้ว reload หน้า ยังเห็นหน้าเข้าสู่ระบบ ไม่หลุดเข้าหน้าหลัก
- [x] T6.5 ตรวจว่าไม่มี UI สมัครสมาชิกเองหรือรีเซ็ตรหัสผ่าน — ตรวจโค้ด `LoginForm.jsx` และหน้าจอจริง มีแค่ช่องอีเมล/รหัสผ่าน + ปุ่มเข้าสู่ระบบ ไม่มีลิงก์สมัคร/ลืมรหัสผ่าน
- [x] T6.6 ตรวจว่ายังไม่มีปุ่มลบ Loan เหมือนเดิม และฟีเจอร์นอกขอบเขตอื่น ๆ ในหัวข้อ 2 ของ design.md ยังไม่ถูกเพิ่ม — ตรวจโค้ด `LoanItem.jsx`/`loansApi.js` ไม่มีปุ่มลบหรือ policy `delete` บนตาราง `loans`

- **เสร็จเมื่อ:** ทดสอบครบทุกข้อในเฟส 6 ผ่านหมด — **เสร็จสมบูรณ์** เวอร์ชัน 2 พร้อมใช้งาน
- **หมายเหตุ:** เก็บบัญชีทดสอบ `owner2-test@borrowbuddy.local` ไว้ในระบบต่อตามที่ผู้ใช้ขอ สำหรับทดสอบ RLS ในอนาคต (รหัสผ่านไม่บันทึกไว้ในเอกสารนี้ เพราะ repo เป็น public — ดูได้จากประวัติแชท)