# Borrow Buddy: เอกสารออกแบบ (เวอร์ชัน 2)

ศัพท์ทั้งหมดในเอกสารนี้อ้างอิงจาก [CONTEXT.md](./CONTEXT.md)

> **หมายเหตุเวอร์ชัน 2**: เปลี่ยนจากเก็บข้อมูลใน localStorage เป็น Supabase และเพิ่มการล็อกอินสำหรับเจ้าของ รายละเอียดที่เปลี่ยนอยู่ในหัวข้อ 3, 4, 6, 7, 8 และ 9 ส่วนที่เหลือ (กติกาการยืม-คืน, ขอบเขตฟีเจอร์การยืม) ยังเหมือนเวอร์ชันแรก

## 1. ภาพรวมและเป้าหมาย
เว็บหน้าเดียวสำหรับ **เจ้าของ** ใช้บันทึกว่า **เพื่อน** ยืม **ของ** อะไร เมื่อไร ต้องคืนเมื่อไร และกดว่า **คืนแล้ว** ได้ เจ้าของต้อง **เข้าสู่ระบบ** ก่อนใช้งาน และเห็นเฉพาะ Loan ของบัญชีตัวเอง

## 2. ขอบเขต

**อยู่ในขอบเขต**
- เพิ่ม Loan, แก้ไข Loan, ดูรายการ Loan
- กดคืนแล้ว (ระบุวันที่คืนจริง) และยกเลิกการคืน
- ค้นหา/กรองตามชื่อเพื่อน
- แสดงสถานะ และจำนวนวันเกินกำหนด
- เข้าสู่ระบบด้วยอีเมล + รหัสผ่าน (ใหม่ในเวอร์ชัน 2)
- แต่ละเจ้าของเห็นเฉพาะ Loan ของตัวเอง ผ่าน RLS (ใหม่ในเวอร์ชัน 2)

**นอกขอบเขต**
- การสมัครสมาชิกเอง (self sign-up) — สร้างบัญชีเจ้าของได้จากฝั่งผู้ดูแลระบบเท่านั้น (เช่นผ่าน Supabase Dashboard)
- ลืมรหัสผ่าน/รีเซ็ตรหัสผ่านผ่านหน้าเว็บ
- สิทธิ์หลายระดับ (role/permission), การแชร์ Loan ข้ามบัญชี
- ลบ Loan, แจ้งเตือน, เลื่อนกำหนดคืน, รูปภาพของ
- ทะเบียนรายชื่อเพื่อน/ของแยก, การตรวจของซ้ำซ้อน
- สำรองข้อมูล/นำเข้า-ส่งออกไฟล์

## 3. เทคโนโลยี
- React + Vite, ภาษา JavaScript (ไม่ใช้ TypeScript)
- เก็บข้อมูลใน **Supabase** (Postgres + Supabase Auth) แทน localStorage
- ยืนยันตัวตนด้วย Supabase Auth (email + password), ปิดการสมัครสมาชิกเอง (sign-up) ในฝั่ง client
- ทดสอบด้วย Vitest (ตรรกะใน `lib/` ยังทดสอบแบบไม่ต้องต่อ Supabase จริง โดย mock การเรียก client)

## 4. โมเดลข้อมูล

**Loan** (ตาราง `loans` ใน Supabase)
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid | สร้างอัตโนมัติ (default `gen_random_uuid()`) |
| `owner_id` | uuid | อ้างอิง `auth.users.id` ของเจ้าของ Loan นี้ บังคับกรอก ใหม่ในเวอร์ชัน 2 |
| `friend_name` | text | ชื่อเล่นของเพื่อน พิมพ์เองทุกครั้ง ห้ามว่าง |
| `item_name` | text | ชื่อของ ห้ามว่าง |
| `borrowed_date` | date | วันที่ยืม |
| `due_date` | date | กำหนดคืน บังคับกรอก |
| `returned_date` | date หรือ `null` | วันที่คืนจริง ว่างจนกว่าจะกดคืน |

ชื่อฟิลด์ใช้ `snake_case` ตามธรรมเนียมตาราง Postgres/Supabase (ต่างจากเวอร์ชันแรกที่ใช้ `camelCase` ใน localStorage) โค้ดฝั่ง UI แปลงเป็น `camelCase` ในชั้น `lib/` เดิม

วันที่เก็บเป็น ค.ศ. แบบ ISO และแสดงผลเป็น พ.ศ. เท่านั้น (เช่น 24 ก.ย. 2569) สถานะและจำนวนวันเกินกำหนดไม่เก็บ คำนวณจากวันที่ทุกครั้ง `owner_id` ถูกตั้งค่าอัตโนมัติจากผู้ใช้ที่ล็อกอินอยู่ ไม่ให้ผู้ใช้แก้ไขเอง

## 5. กติกา

**สถานะ** (คำนวณจากวันนี้)
- **คืนแล้ว**: `returnedDate` ไม่เป็น null
- **เกินกำหนด**: ยังไม่คืน และวันนี้ > `dueDate`
- **ยังไม่คืน**: ยังไม่คืน และวันนี้ ≤ `dueDate` (ถึงกำหนดวันนี้พอดียังไม่เกิน)

**จำนวนวันเกินกำหนด** = วันนี้ − `dueDate` (นับเป็นวัน ใช้กับ Loan ที่เกินกำหนดเท่านั้น)

**การตรวจวันที่** (ปฏิเสธการบันทึกเมื่อผิดกติกา พร้อมข้อความภาษาไทย)
- `dueDate` ต้องไม่ก่อน `borrowedDate`
- `returnedDate` ต้องไม่ก่อน `borrowedDate`
- ชื่อเพื่อนและชื่อของห้ามว่าง

**การกดคืนแล้ว**
- ค่าเริ่มต้นของวันที่คืนจริงคือวันนี้ แก้เป็นวันย้อนหลังได้
- ยกเลิกการคืนได้ (ตั้ง `returnedDate` กลับเป็น null)

## 6. หน้าจอ

**หน้าเข้าสู่ระบบ** (ใหม่ในเวอร์ชัน 2)
- ช่องกรอกอีเมลและรหัสผ่าน, ปุ่มเข้าสู่ระบบ
- แสดงข้อความผิดพลาดภาษาไทยเมื่อล็อกอินไม่สำเร็จ
- ไม่มีลิงก์สมัครสมาชิกหรือลืมรหัสผ่านในหน้านี้ (นอกขอบเขต)
- ยังไม่ล็อกอิน หรือ session หมดอายุ → เด้งมาหน้านี้เสมอ

**หน้าหลัก (หลังล็อกอิน)**

- ปุ่ม **ออกจากระบบ** และแสดงอีเมลเจ้าของที่ล็อกอินอยู่ (ใหม่ในเวอร์ชัน 2)
- **ฟอร์ม Loan** ด้านบน ใช้ทั้งเพิ่มและแก้ไข: เพื่อน, ของ, วันที่ยืม (ค่าเริ่มต้นวันนี้), กำหนดคืน กดแก้ไขที่รายการแล้วข้อมูลขึ้นฟอร์ม มีปุ่มยกเลิกการแก้ไข
- **ช่องค้นหา/กรอง** ตามชื่อเพื่อน
- **รายการ Loan** ด้านล่าง แยกกลุ่มตามลำดับ **เกินกำหนด → ยังไม่คืน → คืนแล้ว** แต่ละกลุ่มเรียงตามกำหนดคืนใกล้สุดก่อน
- แต่ละรายการแสดง: ชื่อของ, ชื่อเพื่อน, วันที่ยืม, กำหนดคืน, สถานะ, จำนวนวันเกินกำหนด (เฉพาะเกินกำหนด), วันที่คืนจริง (เฉพาะคืนแล้ว)
- ปุ่มต่อรายการ: **คืนแล้ว** (เฉพาะที่ยังไม่คืน), **ยกเลิกการคืน** (เฉพาะที่คืนแล้ว), **แก้ไข**
- ไม่มีปุ่มลบ

**การแสดงผล**
- ภาษาไทยทั้งหมด, วันที่ พ.ศ.
- รองรับหน้าจอมือถือ (layout ยืดหยุ่น)
- โหมดมืด สลับได้ด้วยปุ่ม และจำค่าที่เลือกไว้
- ใช้สี/ข้อความแยกสถานะให้ชัด ไม่พึ่งสีอย่างเดียว

## 7. โครงสร้างโค้ด (ระดับโฟลเดอร์)

แยกตรรกะล้วนออกจาก UI เพื่อให้ทดสอบง่าย

```
borrow-buddy/
├── CONTEXT.md
├── design.md
├── package.json
├── index.html
└── src/
    ├── main.jsx
    ├── App.jsx              # ประกอบหน้า สลับหน้า login/หน้าหลักตามสถานะล็อกอิน
    ├── components/
    │   ├── LoginForm.jsx    # ใหม่ในเวอร์ชัน 2: ฟอร์มอีเมล + รหัสผ่าน
    │   ├── LoanForm.jsx
    │   ├── LoanList.jsx
    │   ├── LoanItem.jsx
    │   ├── SearchBox.jsx
    │   └── ThemeToggle.jsx
    └── lib/
        ├── loanRules.js     # สถานะ, จำนวนวันเกิน, ตรวจวันที่, เรียง/จัดกลุ่ม (ไม่เปลี่ยน)
        ├── dateFormat.js    # แปลง ISO ค.ศ. เป็นข้อความ พ.ศ. (ไม่เปลี่ยน)
        ├── supabaseClient.js # ใหม่ในเวอร์ชัน 2: สร้าง Supabase client เดียวใช้ทั้งแอป
        ├── auth.js           # ใหม่ในเวอร์ชัน 2: เข้าสู่ระบบ/ออกจากระบบ, subscribe สถานะ session
        └── loansApi.js       # ใหม่ในเวอร์ชัน 2: อ่าน/เขียน Loan ผ่าน Supabase (แทน storage.js)
```

`storage.js` ของเวอร์ชันแรกถูกแทนที่ด้วย `loansApi.js` (คุยกับ Supabase แทน localStorage) ฟังก์ชันตรรกะล้วนใน `loanRules.js` และ `dateFormat.js` ยังคงรับ "วันนี้" เป็นพารามิเตอร์ ไม่เรียกเวลาจริงข้างใน เพื่อให้ทดสอบได้แน่นอน เหมือนเดิม

## 8. การเก็บข้อมูลและการยืนยันตัวตน

**การเก็บข้อมูล (เปลี่ยนจากเวอร์ชันแรก)**
- ย้ายจาก localStorage ไปตาราง `loans` ใน Supabase (Postgres)
- บันทึกทุกครั้งที่ Loan เปลี่ยน (insert/update ผ่าน Supabase client) โหลดรายการของเจ้าของที่ล็อกอินอยู่ตอนเปิดหน้า
- ถ้าเรียก Supabase ไม่สำเร็จ (เครือข่ายขัดข้อง/สิทธิ์ไม่พอ) ให้แจ้งเจ้าของเป็นภาษาไทย และไม่เขียนทับข้อมูลเดิมในหน้าจอโดยไม่แจ้ง (แนวคิดเดียวกับการจัดการ JSON เสียในเวอร์ชันแรก)
- ข้อมูลอยู่บน Supabase กลาง ไม่ผูกกับเครื่อง/เบราว์เซอร์อีกต่อไป เข้าได้จากหลายอุปกรณ์เมื่อล็อกอินด้วยบัญชีเดียวกัน

**การยืนยันตัวตน (ใหม่ในเวอร์ชัน 2)**
- ใช้ Supabase Auth แบบ email + password
- ปิดการสมัครสมาชิกเอง: ไม่มี UI สมัครสมาชิก สร้างบัญชีเจ้าของทำผ่านฝั่งผู้ดูแลระบบเท่านั้น (เช่น Supabase Dashboard หรือ Admin API)
- หน้าเว็บตรวจสถานะ session ตอนเปิดหน้า ถ้ายังไม่ล็อกอินให้แสดงหน้าเข้าสู่ระบบ
- ออกจากระบบล้าง session และกลับไปหน้าเข้าสู่ระบบ

**Row Level Security (RLS) — ใหม่ในเวอร์ชัน 2**
- เปิด RLS บนตาราง `loans`
- Policy สำหรับ `select`, `insert`, `update` (ไม่มี `delete` เพราะยังไม่มีการลบ Loan) จำกัดด้วย `owner_id = auth.uid()`
- `insert` ต้องบังคับ `owner_id` ให้ตรงกับ `auth.uid()` ของผู้ใช้ที่ยิง request ป้องกันการสร้าง Loan ให้บัญชีอื่น
- ผลคือแต่ละเจ้าของเห็นและแก้ไขได้เฉพาะ Loan ของตัวเอง แม้ระบบจะมีหลายบัญชีในอนาคต

## 9. การทดสอบ (Vitest)
- ตรรกะใน `loanRules.js` และ `dateFormat.js` ทดสอบเหมือนเวอร์ชันแรก ไม่ต้องต่อ Supabase จริง:
  - สถานะ: ยังไม่คืน / เกินกำหนด / คืนแล้ว
  - ขอบเขต: วันนี้ = กำหนดคืน ต้องเป็นยังไม่คืน, วันถัดไปต้องเป็นเกินกำหนด
  - จำนวนวันเกินกำหนด
  - การตรวจวันที่ทุกกรณีที่ผิดกติกา
  - การจัดกลุ่มและเรียงลำดับ
  - การแปลงวันที่เป็น พ.ศ.
  - การกรองตามชื่อเพื่อน
- `loansApi.js` และ `auth.js` (ใหม่): เทสต์โดย mock Supabase client เช่น ตรวจว่าเรียก `.from('loans').select()` ถูกกรองด้วยผู้ใช้ปัจจุบัน และเมื่อเรียกล้มเหลวให้คืนข้อความแจ้งเตือนภาษาไทยตามที่ออกแบบไว้ ไม่ต้องมี Supabase project จริงระหว่างรัน `npm test`

## 10. ประเด็นที่ตั้งใจไม่ทำ
สำรองข้อมูล, ลบ Loan, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, ทะเบียนเพื่อน/ของ, การสมัครสมาชิกเอง, ลืม/รีเซ็ตรหัสผ่านผ่านหน้าเว็บ, สิทธิ์หลายระดับ, การแชร์ Loan ข้ามบัญชี หากต้องการในอนาคตให้ทบทวน CONTEXT.md ก่อน
